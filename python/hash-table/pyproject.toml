[tool.poetry]
name = "hash-table"
version = "0.1.0"
description = ""
authors = ["Taylor <taylorwhite21@yahoo.com>"]

[tool.poetry.dependencies]
python = "^3.9"
pytest = "^7.0.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
